[project]
name = "publish-thingiverse"
version = "0.1.0"
description = "Thingiverse draft uploads through a dedicated browser session"
requires-python = ">=3.11"
dependencies = ["playwright>=1.55,<2"]

[dependency-groups]
dev = ["ruff>=0.12", "pyyaml>=6"]

[tool.uv]
package = false
