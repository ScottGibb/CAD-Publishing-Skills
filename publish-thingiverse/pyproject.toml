[project]
name = "publish-thingiverse"
version = "0.1.0"
description = "Thingiverse draft uploads using the documented REST API"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28,<1"]

[dependency-groups]
dev = ["ruff>=0.12", "pyyaml>=6"]

[tool.uv]
package = false
