[project]
name = "publish-3d-model-release"
version = "0.1.0"
description = "Draft-only 3D model publication helpers"
requires-python = ">=3.11"
dependencies = [
  "google-api-python-client>=2.0",
  "google-auth-oauthlib>=1.2",
]

[tool.uv]
package = false
