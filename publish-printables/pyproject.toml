[project]
name = "publish-printables"
version = "0.2.0"
description = "Prepare Printables manual upload packages with validated release assets"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["patchright>=1.55,<2", "ruff>=0.12", "pyyaml>=6"]

[tool.uv]
package = false
