[project]
name = "publish-youtube"
version = "0.1.0"
description = "Private CAD video uploads using the YouTube Data API"
requires-python = ">=3.11"
dependencies = ["google-auth-oauthlib>=1.2,<2"]

[dependency-groups]
dev = ["ruff>=0.12", "pyyaml>=6"]

[tool.uv]
package = false
